#include "monty.h"
#include "monty.h"
/**
 * f_nop - Function that does nothing
 * @head: Pointer to the head of the stack
 * @counter: Line number
 *
 * Description: This function does nothing and is
 * used as a placeholder.It takes the stack head and the
 * line number as parameters.
 */

void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
